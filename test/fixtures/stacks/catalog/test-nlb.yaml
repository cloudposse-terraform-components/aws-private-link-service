components:
  terraform:
    test-nlb:
      vars:
        enabled: true
        name: "test-nlb"
        internal: true
        load_balancer_type: "network"
        cross_zone_load_balancing_enabled: true
        deletion_protection_enabled: false
