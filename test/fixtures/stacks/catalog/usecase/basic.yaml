components:
  terraform:
    private-link-service/basic:
      metadata:
        component: private-link-service
      vars:
        enabled: true
        name: "private-link-service"
        vpc_endpoint_service_acceptance_required: false
        vpc_endpoint_service_network_load_balancer_arns:
          - !terraform.state nlb 'nlb.nlb_arn'
